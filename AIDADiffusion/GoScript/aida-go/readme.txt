experimental
-currently do not have all the function/features of the old react ui
-this is new ui written in go, to use this, copy aida-go.exe to directory same as "staticserver.exe"
-to build from source, you will need to install fyne and mingw64 then "CGO_ENABLED=1 go build ." in terminal


todo
-fix fallback_img path
-add iteration num
-add img size settings
-add advanced mode
-add select img window for img2img
-add img2img
